_samples/
__pycache__/
*.pyc
.ruff_cache/
.venv/
.DS_Store
Thumbs.db

# Build cache (fetched by scripts/build_site_data.py on demand — not shipped)
data/krcg_vtes.json

# Legacy user scans (build-time input only — not served by the site)
images/scan/

# Local dev staging
_site/

# Editor (local F5 dev profile — not shared)
.vscode/

# Node tooling (lint/format/test only — the site itself has no JS deps)
node_modules/
npm-debug.log*
.eslintcache

# c8 coverage output (generated by `npm run test:coverage`)
coverage/
